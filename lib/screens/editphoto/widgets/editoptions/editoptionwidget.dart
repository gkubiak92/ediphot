import 'package:flutter/material.dart';

class EditOptionWidget extends StatelessWidget {
  final String name;

  const EditOptionWidget({Key key, this.name}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
